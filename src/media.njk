---
layout: base.njk
title: Media
description: Watch Dr. Raj Shah's talks and presentations on innovative math education
---

<section class="py-16 lg:py-24">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <h1 class="text-4xl lg:text-5xl font-bold mb-4 text-primary">Media</h1>
    <p class="text-xl mb-12">Watch talks and presentations on transforming math education</p>
    
    {# Videos Section #}
    <div class="grid md:grid-cols-2 gap-8 mb-20">
      {% for video in media.videos %}
      <div class="card bg-base-100 shadow-xl rounded-2xl h-full">
        <div class="card-body flex flex-col">
          <h2 class="card-title text-2xl">{{ video.title }}</h2>
          <p>{{ video.description }}</p>
          {% video video.platform, video.videoId, video.title %}
          
          {% if video.keyMoments %}
          <div class="timestamp-links">
            <p class="font-semibold mb-2">Key Moments:</p>
            {% for moment in video.keyMoments %}
            {% if video.platform == "youtube" %}
            <a href="https://www.youtube.com/watch?v={{ video.videoId }}&t={{ moment.seconds }}" target="_blank" class="timestamp-link" rel="noopener noreferrer">
              <span class="time">{{ moment.time }}</span>
              <span class="description">{{ moment.description }}</span>
            </a>
            {% elif video.platform == "vimeo" %}
            <a href="https://vimeo.com/{{ video.videoId }}#t={{ moment.seconds }}s" target="_blank" class="timestamp-link" rel="noopener noreferrer">
              <span class="time">{{ moment.time }}</span>
              <span class="description">{{ moment.description }}</span>
            </a>
            {% endif %}
            {% endfor %}
          </div>
          {% endif %}
        </div>
      </div>
      {% endfor %}
    </div>

    {# Podcasts Section #}
    <div class="mb-8">
      <h2 class="text-4xl font-bold mb-2 text-primary">Podcast Appearances</h2>
      <p class="text-xl mb-12">Listen to conversations about transforming math education</p>
    </div>

    <div class="grid md:grid-cols-2 gap-8">
      {% for episode in media.podcasts %}
      <div class="card bg-base-100 shadow-xl rounded-2xl">
        <div class="card-body">
          <h3 class="card-title text-2xl mb-2">{{ episode.title }}</h3>
          <p class="text-sm text-base-content/70 mb-4">{{ episode.podcastName }} • {{ episode.date }}</p>
          
          {% podcast episode.url %}
          
          <div class="timestamp-links">
            <p class="font-semibold mb-2">Key Moments:</p>
            {% for moment in episode.keyMoments %}
            <a href="{{ episode.url }}&t={{ moment.seconds }}" target="_blank" class="timestamp-link" rel="noopener noreferrer">
              <span class="time">{{ moment.time }}</span>
              <span class="description">{{ moment.description }}</span>
            </a>
            {% endfor %}
          </div>
        </div>
      </div>
      {% endfor %}
    </div>
  </div>
</section>
