---
layout: base.njk
title: Making Math Irresistible
---

{# Hero Section #}
<section class="relative py-16 lg:py-24">
  {# Background image for large screens only #}
  <div class="hidden lg:block absolute inset-0 z-0">
    <img src="/images/CAMT 3.jpg" alt="Math classroom" class="w-full h-full object-cover">
    <div class="absolute inset-0 bg-gradient-to-r from-base-100/0 via-base-100/60 to-base-100/90"></div>
  </div>
  
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    <div class="grid lg:grid-cols-2 gap-12 items-center">
      {# Image shows on mobile, hidden on large screens #}
      <div class="aspect-video rounded-lg overflow-hidden lg:hidden">
        <img src="/images/CAMT 3.jpg" alt="Math classroom" class="w-full h-full object-cover">
      </div>
      {# Empty left column on large screens #}
      <div class="hidden lg:block"></div>
      {# Text in right column #}
      <div>
        <h1 class="text-4xl lg:text-6xl font-bold mb-4 text-primary">{{ site.title }}</h1>
        <h2 class="text-2xl lg:text-3xl font-semibold mb-6 text-primary">Get kids EXCITED about Math</h2>
        <p class="text-xl text-primary mb-8">{{ site.description | safe }}</p>
        <div class="flex flex-wrap gap-4">
          <a href="#book" class="btn btn-primary btn-lg">Book a Workshop</a>
          <a href="/math-resources" class="btn btn-lg btn-secondary">Math Resources</a>
        </div>
      </div>
    </div>
  </div>
</section>

{# Trust Logos Bar #}
<section class="bg-base-200 py-8">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex flex-wrap justify-center items-center gap-8 lg:gap-16">
      <img src="/images/logo/MPA.png" alt="Math Plus Academy" class="h-12 w-auto">
      <img src="/images/logo/MHE.png" alt="McGraw Hill Education" class="h-12 w-auto">
      <img src="/images/logo/TEDx.png" alt="TEDx" class="h-12 w-auto">
      <img src="/images/logo/GMP.png" alt="Global Math Project" class="h-12 w-auto">
      <img src="/images/logo/RDSD.png" alt="Reynoldsburg Schools" class="h-12 w-auto">
      <img src="/images/logo/NCTM.png" alt="National Council of Teachers of Mathematics" class="h-12 w-auto">
    </div>
  </div>
</section>

{# Services Section #}
<section class="bg-gradient-to-br from-primary/90 to-primary/30 py-16 lg:py-24">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="grid md:grid-cols-3 gap-8">
      {% for service in services %}
      <div class="card bg-base-100 shadow-xl rounded-2xl">
        <div class="card-body">
          <h3 class="card-title text-2xl text-primary flex items-start gap-3">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-7 h-7 text-primary flex-shrink-0">
              <path stroke-linecap="round" stroke-linejoin="round" d="{{ service.icon }}" />
            </svg>
            {{ service.title }}
          </h3>
          <p>{{ service.description | safe }}</p>
          <a href="{{ service.url | safe }}" class="btn btn-lg btn-sm btn-outline btn-primary">{{ service.button }}</a>
        </div>
      </div>
      {% endfor %}
    </div>
  </div>
</section>

{# About Section #}
<section class="py-16 lg:py-24">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="grid lg:grid-cols-3 gap-12 items-start">
      <div class="lg:col-span-1 flex justify-center items-center">
        <div class="avatar">
          <div class="w-64 rounded-full">
            <img src="/images/Raj-Shah-headshot.png" alt="Dr. Raj Shah">
          </div>
        </div>
      </div>
      <div class="lg:col-span-2">
        <h2 class="text-3xl text-primary font-bold mb-6">I'm on a mission to make math irresistible.</h2>
        <div class="prose lg:prose-lg">
          <p>Every day computation skills become less valuable and thinking become more valuable. School math over values memorization and under emphasizes thinking skills.</p>
          <p>I have always had an affinity for math. Powered by my love of math, I earned a Ph.D. in Physics in 1999 which led to a decade in R&D at Intel. In 2008, I quit my job and founded Math Plus Academy, an after-school STEM enrichment program. I'm also proud to be a founding member of The Global Math Project.</p>
        </div>
      </div>
    </div>
  </div>
</section>

{# Philosophy Section #}
<section class="bg-gradient-to-br from-secondary/30 to-secondary/90 py-16 lg:py-24">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="grid md:grid-cols-3 gap-8">
      {% for pillar in philosophy %}
      <div class="card bg-base-100 shadow-xl rounded-2xl">
        <div class="card-body">
          <h3 class="card-title text-2xl">{{ pillar.title }}</h3>
          <p>{{ pillar.description }}</p>
        </div>
      </div>
      {% endfor %}
    </div>
  </div>
</section>

{# Book a Workshop Section #}
<section id="book" class="bg-primary text-primary-content py-16 lg:py-24">
  <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <h2 class="text-4xl font-bold mb-6">Ready to Transform Your Math Classroom?</h2>
    <p class="text-xl mb-8">Book a workshop or keynote to bring joy, curiosity, and rigor to your school or conference.</p>
    <a href="{{ site.author.booking_link }}" class="btn btn-secondary btn-lg">Book a Consultation</a>
  </div>
</section>

{# Testimonials Carousel #}
<section class="bg-base-200 py-16 lg:py-24">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="relative overflow-hidden">
      <div id="testimonial-carousel" class="flex transition-transform duration-500 ease-in-out">
        {% for testimonial in testimonials %}
        <div class="w-full md:w-1/2 flex-shrink-0 px-4">
          <div class="card bg-base-100 shadow-xl rounded-2xl">
            <div class="card-body">
              <div class="flex mb-2">
                {% for i in range(0, testimonial.rating) %}
                <span class="text-warning text-xl">★</span>
                {% endfor %}
              </div>
              <p class="text-lg mb-4">"{{ testimonial.quote }}"</p>
              <div class="text-sm">
                <p class="font-semibold">– {{ testimonial.author }}</p>
                <p class="text-base-content/70">– {{ testimonial.organization }}</p>
              </div>
            </div>
          </div>
        </div>
        {% endfor %}
        {# Duplicate testimonials for infinite loop #}
        {% for testimonial in testimonials %}
        <div class="w-full md:w-1/2 flex-shrink-0 px-4">
          <div class="card bg-base-100 shadow-xl rounded-2xl">
            <div class="card-body">
              <div class="flex mb-2">
                {% for i in range(0, testimonial.rating) %}
                <span class="text-warning text-xl">★</span>
                {% endfor %}
              </div>
              <p class="text-lg mb-4">"{{ testimonial.quote }}"</p>
              <div class="text-sm">
                <p class="font-semibold">– {{ testimonial.author }}</p>
                <p class="text-base-content/70">– {{ testimonial.organization }}</p>
              </div>
            </div>
          </div>
        </div>
        {% endfor %}
      </div>
    </div>
  </div>
</section>

<script>
  (function() {
    const carousel = document.getElementById('testimonial-carousel');
    const totalSlides = {{ testimonials | length }};
    let currentIndex = 0;
    let isTransitioning = false;

    function nextSlide() {
      if (isTransitioning) return;
      isTransitioning = true;
      
      currentIndex++;
      carousel.style.transition = 'transform 500ms ease-in-out';
      carousel.style.transform = `translateX(-${currentIndex * 50}%)`;
      
      // Reset to beginning when we reach the duplicate set
      if (currentIndex >= totalSlides) {
        setTimeout(() => {
          carousel.style.transition = 'none';
          currentIndex = 0;
          carousel.style.transform = 'translateX(0)';
          setTimeout(() => {
            isTransitioning = false;
          }, 50);
        }, 500);
      } else {
        setTimeout(() => {
          isTransitioning = false;
        }, 500);
      }
    }

    // Auto-advance every 5 seconds
    setInterval(nextSlide, 5000);
  })();
</script>
